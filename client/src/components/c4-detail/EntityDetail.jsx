// Person 4 (C4) — implement EntityDetail
export default function EntityDetail({ item }) {
  return (
    <div>
      <p>TODO: Person 4 — EntityDetail component</p>
      <pre>{JSON.stringify(item, null, 2)}</pre>
    </div>
  );
}
