// Person 4 (C4) — Product detail component with Add to Cart (FR2)
export default function ProductDetail({ product }) {
  return (
    <div>
      <p>TODO: Person 4 — ProductDetail component</p>
      <pre>{JSON.stringify(product, null, 2)}</pre>
    </div>
  );
}
