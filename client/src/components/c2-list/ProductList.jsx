// Person 2 (C2) — Product grid component (FR1)
export default function ProductList({ products = [] }) {
  return (
    <div>
      <p>TODO: Person 2 — ProductList component</p>
      <pre>{JSON.stringify(products, null, 2)}</pre>
    </div>
  );
}
