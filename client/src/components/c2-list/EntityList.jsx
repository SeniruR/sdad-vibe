// Person 2 (C2) — implement EntityList
export default function EntityList({ items = [] }) {
  return (
    <div>
      <p>TODO: Person 2 — EntityList component</p>
      <pre>{JSON.stringify(items, null, 2)}</pre>
    </div>
  );
}
